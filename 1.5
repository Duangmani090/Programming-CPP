//# include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {

  char op;
  float num1, num2;

  cout << "Enter frist operands: ";
  cout >> num1 >> num2
  num2;
  cout << "Enter second operands: ";
  switch(op) {

    case '+':
      result = num1 + num2;
      cout << num1, << num2,
      break;
    case '-':
       result = num1 - num2;
      break;
    case '*':
      result = num1 * num2;
      break;

    case '/':
       result = num1 / num2;
       cout << num1 "+" << num2 ""
      break;
	
	// Printing the result
    if (res != -DBL_MAX)
        cout <<num1 << op <<num2 << "/ "<< result;
  return 0;
}
